/**
 * The middleware that loads message modules.
 */
export const MessagesMiddleware = 'i18n:messages'

/**
 * The global middleware that loads initial message modules.
 */
export const InitialModulesMiddleware = 'i18n:initial'
